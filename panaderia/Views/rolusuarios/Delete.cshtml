@model panaderia.Models.rolusuario

@{
    ViewBag.Title = "Elminar Rol";
}

<h2>Gestión Eliminar Rol Usuario</h2>

<h3></h3>
<div>
    <h4></h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.Id)

    <!--  Tablas   -->
    <div class="form-group">
        <label class="col-md-3" ">Codigo Rol</label>
        <div class="col-md-offset-2">
            @Html.DisplayFor(model => model.Id)
        </div>
    </div>


    <div class="form-group">
        <label class="col-md-3">Nombre Rol</label>
        <div class="col-md-offset-2">
            @Html.DisplayFor(model => model.rol)
        </div>
    </div>




    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="col-md-offset-3">
            <input type="submit" value="Eliminar" class="btn btn-default " id="btnEliminar" /> 
            
        </div>
    }

    <div>
   
       @Html.ActionLink("Volver", "Index")
    </div>


</div>
