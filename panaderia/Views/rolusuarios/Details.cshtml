@model panaderia.Models.rolusuario

@{
    ViewBag.Title = "Detalles";
}

<h2>Gestión Detalle Rol Usuario</h2>

<div>
    <h4></h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.Id)



    <!--  Tablas   -->
    <div class="form-group">
        <label class="col-md-3">Nombre Rol</label>
        <div class="col-md-offset-2">
            @Html.DisplayFor(model => model.rol)
        </div>
    </div>
</div>
<p>

    <div class="col-md-offset-3">
        <button type="button" class="btn-default">@Html.ActionLink("Editar", "Edit", new { id = Model.Id })  </button> |
        <button type="button" class="btn-default">@Html.ActionLink("Volver", "Index")</button>

        </div>

</p>
