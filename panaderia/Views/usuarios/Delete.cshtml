@model panaderia.Models.usuario

@{
    ViewBag.Title = "Eliminar Uuario";
}

<h2>Eliminar</h2>

<div>
    <h4>Gestión Eliminar Usuario</h4>
    <hr />
    <dl class="dl-horizontal">


        <dt>
            @Html.DisplayNameFor(model => model.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.clave)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.clave)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.rolusuario.rol)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.rolusuario.rol)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Eliminar" class="btn" /> |        

        <button type="button" class="btn">@Html.ActionLink("Volver", "Index")</button>
    </div>
    }
</div>
